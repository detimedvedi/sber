# 🔍 АУДИТ КОДА: Система обнаружения аномалий СберИндекс

**Дата аудита:** 6 ноября 2025  
**Проведён:** Независимым аудитором  
**Объект аудита:** AI-generated код для конкурса СберИндекс

---

## 📋 EXECUTIVE SUMMARY

**Общая оценка:** ⚠️ **4/10 - Требуется серьёзная доработка**

### Критические проблемы:
1. ❌ **Фундаментальное непонимание структуры данных** - алгоритмы не соответствуют формату
2. ❌ **Неверная логика кросс-источникового сравнения** - сравниваются несопоставимые метрики
3. ❌ **Игнорирование временной структуры** - агрегация уничтожает временные паттерны
4. ❌ **Граф связей не используется** - connection.parquet загружается, но не анализируется
5. ⚠️ **Множественные статистические ошибки** - неправильное применение робастных метрик

### Что работает:
- ✅ Базовая архитектура кода (классы, модули)
- ✅ Статистические методы математически корректны (z-score, IQR, MAD)
- ✅ Обработка пропущенных значений продумана
- ⚠️ Экспорт результатов и визуализация

---

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #1: НЕПОНИМАНИЕ СТРУКТУРЫ ДАННЫХ

### 1.1 Consumption Data - Потеря временной структуры

**Реальная структура данных:**
```python
# consumption.parquet - 303,126 строк
date: "2023-01"         # Временная метка
territory_id: 1         # Территория
category: "Продовольствие"  # Категория
value: 7692            # Значение потребления
```

**Что делает код (`data_loader.py:219-238`):**
```python
consumption_pivot = consumption_df.pivot_table(
    index='territory_id',
    columns='category',
    values='value',
    aggfunc='mean'  # ❌ УНИЧТОЖАЕТ ВРЕМЕННУЮ СТРУКТУРУ
)
```

**Результат:**
- 303,126 строк → 2,571 строк (99% данных потеряно)
- Колонка `date` исчезает
- Невозможен temporal analysis
- Сезонность уничтожена

**Последствия:**
```
Temporal anomalies: 0 detected (disabled)
```

**Правильное решение:**
```python
# Option 1: Latest period only
consumption_latest = consumption_df.sort_values('date').groupby(
    ['territory_id', 'category']
).last()

# Option 2: Keep all periods for temporal analysis
consumption_wide = consumption_df.pivot_table(
    index=['territory_id', 'date'],  # ✅ СОХРАНИТЬ DATE
    columns='category',
    values='value'
)
```

### 1.2 Connection Data - Игнорирование графа связей

**Реальная структура:**
```python
# connection.parquet - 4,761,495 строк
territory_id_x: 2      # Территория 1
territory_id_y: 1      # Территория 2
distance: 100.9        # Расстояние (км)
type: "highway"        # Тип связи
```

**Что делает код:**
```python
# ❌ ВООБЩЕ НЕ ЗАГРУЖАЕТ connection.parquet
# data_loader.py не содержит load_connection_data()
```

**Последствия:**
- 4.7M связей игнорируются
- GeographicAnomalyDetector использует region_name вместо реальных соседей
- "Кластерный анализ" без реального графа соседства

**Правильное решение:**
```python
# Использовать граф для определения соседей
def get_neighbors(territory_id, connections, max_distance=50):
    neighbors = connections[
        ((connections['territory_id_x'] == territory_id) |
         (connections['territory_id_y'] == territory_id)) &
        (connections['distance'] <= max_distance)
    ]
    return neighbors
```

---

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #2: НЕВЕРНОЕ КРОСС-ИСТОЧНИКОВОЕ СРАВНЕНИЕ

**Код определяет пары (`CrossSourceComparator._define_comparable_pairs()`):**
```python
return {
    'consumption_total': 'salary_avg',    # ❌ ОШИБКА!
    'market_access': 'population_total',  # ❌ ОШИБКА!
}
```

### Проблема 2.1: consumption_total vs salary_avg

**Почему это бессмысленно:**
- `consumption_total` = суммарные расходы (еда + здоровье + маркетплейсы + ...)
- `salary_avg` = средняя зарплата по одной отрасли
- ❌ Люди тратят не только зарплату (кредиты, сбережения)
- ❌ Consumption включает туристов, salary - только местных
- ❌ Разные единицы: месячное потребление vs зарплата

**Реальные причины высокого consumption при низком salary:**
- Туристические зоны (Сочи, Крым)
- Деловые центры (Москва-Сити)
- Транзитные территории
- Территории с кредитованием

**Результат:** 2,414 "аномалий" (14.5%), большинство - ложные срабатывания

### Проблема 2.2: market_access vs population_total

**Почему это нелогично:**
- `market_access` - индекс (309.6, 322.8...) - непонятная метрика
- `population_total` - численность населения
- ❌ Нет ожидаемой связи: малонаселенные территории могут иметь хороший доступ
- ❌ Нет методологии расчёта market_access в документации

**Правильный подход:**

Cross-source comparison применим ТОЛЬКО если:
1. Обе метрики измеряют одно и то же (население, зарплата, оборот)
2. Из разных источников (СберИндекс vs Росстат)
3. По одной методологии

**В данном датасете таких пар НЕТ!**

**Рекомендация:** Отключить CrossSourceComparator

---

## 🔴 КРИТИЧЕСКАЯ ПРОБЛЕМА #3: TEMPORAL ANALYSIS НЕ РАБОТАЕТ

**Почему TemporalAnomalyDetector находит 0 аномалий:**

```python
def _has_temporal_data(self, df: pd.DataFrame) -> bool:
    # Ищет колонки: date, period, year, month...
    temporal_columns = ['date', 'period', 'year', 'month']
    for col in temporal_columns:
        if col in df.columns:
            return True  # ❌ НЕТ таких колонок после pivot_table
    
    # Ищет дубликаты territory_id
    territory_counts = df['territory_id'].value_counts()
    if (territory_counts > 1).any():
        return True  # ❌ НЕТ дубликатов после aggfunc='mean'
    
    return False  # ✅ Возвращает False
```

**Результат:**
```
Temporal anomalies: 0 detected (disabled)
```

**Что должно было обнаружиться:**
- Резкие скачки потребления (Январь +200%)
- Сезонность (курорты летом)
- Тренды (постепенный рост)

**Но невозможно:** данные агрегированы до одной точки на территорию!

---

## 🟡 ЗНАЧИТЕЛЬНЫЕ ПРОБЛЕМЫ

### Проблема #4: Geographic Analysis без реального графа

**GeographicAnomalyDetector.detect_cluster_outliers():**
```python
# ❌ НЕВЕРНО: использует регион как кластер
grouped = df.groupby('region_name')

# ✅ ПРАВИЛЬНО: использовать граф связей
neighbors = get_neighbors_from_graph(territory_id, connections)
```

### Проблема #5: Statistical Outlier - низкие пороги

**Текущие настройки:**
```yaml
statistical:
  z_score: 3.0  # Найдёт 0.3% в нормальном распределении
```

**Проблема:** Россия крайне неоднородна (Москва vs Чукотка)
- Естественная вариация >> 3 std
- Нужно z_score >= 5.0

### Проблема #6: Logical Consistency - чрезмерно строгий

**detect_contradictory_indicators:**
```python
# Флагит: "high consumption + low population"
# ❌ Не учитывает туристические зоны
# ❌ Не учитывает деловые центры
```

**Результат:** 4,013 аномалий (24.1%), многие легитимные

---

## 📊 КОЛИЧЕСТВЕННАЯ ОЦЕНКА

### Влияние проблем на False Positives:

| Проблема | Вклад в FP | Критичность |
|----------|-----------|------------|
| Temporal агрегация | 25-30% | 🔴 CRITICAL |
| Cross-source сравнение | 15-20% | 🔴 CRITICAL |
| Граф связей не используется | 10-15% | 🟡 HIGH |
| Geographic без графа | 10-12% | 🟡 HIGH |
| Logical слишком строгий | 8-10% | 🟡 HIGH |
| z_score слишком низкий | 5-8% | 🟠 MEDIUM |

**Суммарно:** ~73-95% false positives

**Текущий результат:** 103% территорий помечены (2,659 из 2,571)

